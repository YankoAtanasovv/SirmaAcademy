function fruitShop(fruit, day, quantityFruit) {
  let price = "";

  switch (day) {
    case "Monday":
      switch (fruit) {
        case "tomato":
          price = (quantityFruit * 2.500).toFixed(2);
          console.log(price);
          break;
        case "onion":
          price = (quantityFruit * 1.200).toFixed(2);
          console.log(price);
          break;
        case "lettuce":
          price = (quantityFruit * 0.85).toFixed(2);
          console.log(price);
          break;
        case "cucumber":
          price = (quantityFruit * 1.45).toFixed(2);
          console.log(price);
          break;
        case "pepper":
          price = (quantityFruit * 5.50).toFixed(2);
          console.log(price);
          break;
        
        default:
          console.log("error");
          break;
      }
      break;

    case "Tuesday":
      switch (fruit) {
        case "tomato":
          price = (quantityFruit * 2.50).toFixed(2);
          console.log(price);
          break;
        case "onion":
          price = (quantityFruit * 1.20).toFixed(2);
          console.log(price);
          break;
        case "lettuce":
          price = (quantityFruit * 0.85).toFixed(2);
          console.log(price);
          break;
        case "cucumber":
          price = (quantityFruit * 1.45).toFixed(2);
          console.log(price);
          break;
        case "pepper":
          price = (quantityFruit * 5.50).toFixed(2);
          console.log(price);
          break;
       
        default:
          console.log("error");
          break;
      }
      break;

    case "Wednesday":
      switch (fruit) {
        case "tomato":
          price = (quantityFruit * 2.50).toFixed(2);
          console.log(price);
          break;
        case "onion":
          price = (quantityFruit * 1.20).toFixed(2);
          console.log(price);
          break;
        case "lettuce":
          price = (quantityFruit * 0.85).toFixed(2);
          console.log(price);
          break;
        case "cucumber":
          price = (quantityFruit * 1.45).toFixed(2);
          console.log(price);
          break;
        case "pepper":
          price = (quantityFruit * 5.50).toFixed(2);
          console.log(price);
          break;
       
        default:
          console.log("error");
          break;
      }
      break;

    case "Thursday":
      switch (fruit) {
        case "tomato":
          price = (quantityFruit * 2.50).toFixed(2);
          console.log(price);
          break;
        case "onion":
          price = (quantityFruit * 1.20).toFixed(2);
          console.log(price);
          break;
        case "lettuce":
          price = (quantityFruit * 0.85).toFixed(2);
          console.log(price);
          break;
        case "cucumber":
          price = (quantityFruit * 1.45).toFixed(2);
          console.log(price);
          break;
        case "pepper":
          price = (quantityFruit * 5.50).toFixed(2);
          console.log(price);
          break;
       
        default:
          console.log("error");
          break;
      }
      break;

    case "Friday":
      switch (fruit) {
        case "tomato":
          price = (quantityFruit * 2.50).toFixed(2);
          console.log(price);
          break;
        case "onion":
          price = (quantityFruit * 1.20).toFixed(2);
          console.log(price);
          break;
        case "lettuce":
          price = (quantityFruit * 0.85).toFixed(2);
          console.log(price);
          break;
        case "cucumber":
          price = (quantityFruit * 1.45).toFixed(2);
          console.log(price);
          break;
        case "pepper":
          price = (quantityFruit * 5.50).toFixed(2);
          console.log(price);
          break;
       
        default:
          console.log("error");
          break;
      }
      break;

    case "Saturday":
      switch (fruit) {
        case "tomato":
          price = (quantityFruit * 2.80).toFixed(2);
          console.log(price);
          break;
        case "onion":
          price = (quantityFruit * 1.30).toFixed(2);
          console.log(price);
          break;
        case "lettuce":
          price = (quantityFruit * 0.85).toFixed(2);
          console.log(price);
          break;
        case "cucumber":
          price = (quantityFruit * 1.75).toFixed(2);
          console.log(price);
          break;
        case "pepper":
          price = (quantityFruit * 3.50).toFixed(2);
          console.log(price);
          break;
       
        default:
          console.log("error");
          break;
      }
      break;

    case "Sunday":
      switch (fruit) {
        case "tomato":
          price = (quantityFruit * 2.80).toFixed(2);
          console.log(price);
          break;
        case "onion":
          price = (quantityFruit * 1.30).toFixed(2);
          console.log(price);
          break;
        case "lettuce":
          price = (quantityFruit * 0.85).toFixed(2);
          console.log(price);
          break;
        case "cucumber":
          price = (quantityFruit * 1.75).toFixed(2);
          console.log(price);
          break;
        case "pepper":
          price = (quantityFruit * 3.50).toFixed(2);
          console.log(price);
          break;
        
        default:
          console.log("error");
          break;
      }
      break;

    default:
      console.log("error");
      break;
  }
}

fruitShop("tomato", "Tuesday", 2);
